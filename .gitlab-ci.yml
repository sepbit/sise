image: node:latest

cache:
  paths:
    - node_modules/

test:
  script:
    - npm install
    - npm run fix-style
    - npm run check-style

pages:
  stage: deploy
  artifacts:
    paths:
      - public
  only:
    - master
